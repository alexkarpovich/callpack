var React = require('react-addons-test-utils');
var expect = require('expect');

describe('example', function () {
  it('this is an example', function () {
    var a = 1;
    expect(a).toExist();
  });
});
